{
  "name": "seleium-bot",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build:dev": "webpack",
    "build:prod": "webpack -p --env production",
    "dev-server": "webpack-dev-server",
    "test-server": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha ./tests/*.test.js",
    "test-client": "jest ./client/tests",
    "start": "node ./server/server.js"
  },
  "author": "Alex Bobbrenko",
  "license": "MIT",
  "dependencies": {
    "chromedriver": "^2.40.0",
    "proxy-verifier": "^0.4.0",
    "selenium-webdriver": "^4.0.0-alpha.1"
  },
  "devDependencies": {
    "expect": "^23.4.0",
    "supertest": "^3.1.0"
  }
}
